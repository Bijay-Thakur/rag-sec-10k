# SEC 10-K RAG — Python stack (CLI + ingestion + OpenAI embeddings + Chroma)

chromadb>=0.4.24,<0.6
openai>=1.40.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
lxml>=5.0.0

# Retrieval
rank-bm25>=0.2.2
sentence-transformers>=3.0.0   # cross-encoder reranker (ms-marco-MiniLM-L-6-v2)

# Dev / testing
pytest>=7.4.0
jupyter>=1.0.0
