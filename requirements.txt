# SEC 10-K RAG — Python stack
# v1 (manual) + v2 (LlamaIndex) pipelines

chromadb>=0.4.24,<0.6
openai>=1.40.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
lxml>=5.0.0

# Retrieval
rank-bm25>=0.2.2
sentence-transformers>=3.0.0   # cross-encoder reranker (ms-marco-MiniLM-L-6-v2)

# Generation
# (uses openai above, no extra deps)

# Evaluation
ragas>=0.4.0              # RAGAS generation eval (installs langchain-core etc.)
datasets>=2.20.0          # HuggingFace datasets — used by ragas
langchain-openai>=0.1.0   # LangChain OpenAI wrapper for ragas metrics
pandas>=2.0.0             # dataframe ops in eval notebooks

# UI
streamlit>=1.35.0

# v2: LlamaIndex pipeline
llama-index-core>=0.14.0
llama-index-embeddings-openai>=0.6.0
llama-index-llms-openai>=0.7.0
llama-index-vector-stores-chroma>=0.5.0
# llama-index-retrievers-bm25  # requires pystemmer (MSVC on Windows); use BM25LlamaRetriever instead

# Dev / testing
pytest>=7.4.0
jupyter>=1.0.0
matplotlib>=3.8.0         # plots in eval notebooks
